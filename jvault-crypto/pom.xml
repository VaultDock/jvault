<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>dev.jvault</groupId>
    <artifactId>jvault-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>jvault-crypto</artifactId>
  <name>jvault-crypto</name>
  <description>
    Envelope encryption for externally stored content: per-object data keys wrapped by a key
    manager, framed authenticated streaming encryption, and the self-describing object header
    (docs/09-encryption.md).
  </description>

  <dependencies>
    <dependency>
      <groupId>dev.jvault</groupId>
      <artifactId>jvault-domain</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!--
      The framed streaming construction is Tink's, not ours. docs/09-encryption.md 9.3 explains
      why: hand-rolling GCM framing is the single most dangerous thing this project could do,
      and Tink's AesGcmHkdfStreaming is audited, binds segment index and the final-segment flag
      into each nonce, and offers a seekable decrypting channel for range reads.
    -->
    <dependency>
      <groupId>com.google.crypto.tink</groupId>
      <artifactId>tink</artifactId>
    </dependency>
  </dependencies>
</project>
